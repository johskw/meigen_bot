<html lang='ja'>
  <head>
    <meta charset='UTF-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <title>名言BOT</title>
  </head>
  <body>
    <a href="/">TOPへ</a>
    <h1>BOTチェッカー</h1>
    <form action="/checker" method="post">
      <input type="text" name="nickname" placeholder="キャラクター名" value="{{ .nickname }}">
      <input type="submit" value="送信">
    </form>
    <p>{{ .meigen }}</p>
  </body>
</html>
